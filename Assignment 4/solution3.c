// Generate the ASCII code of the character '+'

#include<stdio.h>
int main(){
    char ch = '+';
    int ascii_code = (int)ch;
    printf("The ASCII code of character '%c' is: %d\n", ch, ascii_code);
    return 0;
}